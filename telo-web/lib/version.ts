/**
 * Single source of truth for the user-facing app version shown next to the Telo
 * wordmark across the app (top nav, login, landing). Bump this on each release.
 */
export const APP_VERSION = 'v1.84';
